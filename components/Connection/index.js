import Connected from "./Connected";
import TryingToConnect from "./TryingToConnect";
import ErrorConnecting from "./ErrorConnecting";

export default {
  connected: Connected,
  trying: TryingToConnect,
  error: ErrorConnecting,
};
