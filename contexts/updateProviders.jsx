import { createContext } from "react";

export const RelayUpdateContext = createContext((value) => null);
export const SwitchUpdateContext = createContext((value) => null);
export const ZigbeeSwitchUpdateContext = createContext((value) => null);
