import DeviceHeader from "@/components/deviceAndZoneHeader/DeviceHeader";

const Loading = () => {
  return (
    <>
      <DeviceHeader hasPowerButton={false} />
    </>
  );
};

export default Loading;
